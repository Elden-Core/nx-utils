FROM node:24-alpine AS runner

WORKDIR /app

ENV NODE_ENV=production

# NX builds this project to dist/apps/<%= projectName %>/ (webpack/esbuild bundle)
# Run `nx build <%= projectName %>` before docker build
COPY dist/apps/<%= projectName %>/ ./

EXPOSE <%= port %>

CMD ["node", "main.js"]
