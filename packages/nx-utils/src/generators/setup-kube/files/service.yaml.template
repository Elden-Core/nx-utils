apiVersion: v1
kind: Service
metadata:
  name: <%= projectName %>
  namespace: <%= namespace %>
  labels:
    app: <%= projectName %>
spec:
  type: ClusterIP
  selector:
    app: <%= projectName %>
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: <%= port %>
